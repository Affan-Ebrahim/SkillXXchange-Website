# SkillXXchange-Website
#Prerequisites

# Make sure you have the following installed on your system:

#  Node.js (version 14.18+ or 16+ recommended)
 # npm (usually comes with Node.js)

 #Installation
 # Clone the repository or install the files 
 # open the terminal and make its in the project file by typing in this command cd project
 # Then add the following one at a time in the terminal
 # npm install 
 # npm install --save-dev @vitejs/plugin-react vite
 # npm run build
 # npm run preview
